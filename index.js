const board = document.getElementById("board");
const context = board.getContext("2d");

const birdWidth = 34 ;
const birdHeight = 24 ;

const birdX = birdWidth / 8 ;
const birdY = birdHeight / 2 ;

const bird = {
    x : birdX ,
    y : birdY ,
    width : birdWidth ,
    height : birdHeight,
};

window.onload = () => {
    
}

